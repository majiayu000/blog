# 博客内容编辑约定

这些约定用于用户明确要求撰写或修改文章的任务。只改站点代码时，不顺手重写已发表文章。构建、测试和元数据解析继续遵循 CONTRIBUTING.md；本文件不要求构建器改写文章。

## 保留作者的材料

这是个人技术博客。优先保留作者查到的文件、日志、版本、数字、失败样本和有依据的判断，不把调查记录改成行业综述或产品宣传。阅读完整正文，再决定哪里需要重组；只看标题或摘要不够。

已有的第一人称实测可以保留，但不要补写作者没说过的经历、情绪或动机。例如，不能为了“像人写的”添加“我折腾了一整晚”“当时很震惊”“我早就怀疑”。文字修订不等于重新做了一次实验。

事实、代码推断、实际观察要能分清。尤其注意：

- 安装包里有实现，不代表运行时启用；本地有日志，不代表服务端已经收到。
- 没搜到某个字符串，不能证明没有其他实现；没有上传记录，不能证明没有任何数据外发。
- 当前版本、当前设置，不能直接还原历史时点；国内版的配置表也不能代替国际版实测。
- 仅凭客户端行为，不能推断服务端留存、训练用途或厂商动机。

发现原稿结论超出证据时，收紧到证据能支持的范围，并在改动说明中指出；不要悄悄把推测升级成事实。没有原始日志或安装包，就不要宣称完成了独立复核。

## 写成文章，不要拼接短帖

正文用完整自然段，一个段落讲清一个问题，只在话题或论证步骤变化时换段。不要每句话换行，也不要仅靠删换行把互不相关的句子挤在一起。段落长短服从内容，不设置固定字数和固定节数。

开头交代具体发现或正在查的问题。正文沿着材料解释“看到了什么、如何判断、还缺什么”，不必每节都重复这套顺序。小标题指出本节内容，不强制排列成三点、五层或“背景—洞察—启示—总结”。

结尾可以停在检查结果、未解的问题或读者能采取的行动上。没有材料支持时，不追加行业趋势、时代判断和金句。已有表格或图说明过的内容，不再完整复述一遍。

## 把话说具体

技术词、函数名、字段名该留就留，第一次出现时解释与当前问题的关系。不要把整串字段和日志直接摊给读者，却省掉它们为什么支持结论。

避免反复使用“不是……而是……”“真正重要的是”“本质上”“说白了”“这意味着”“值得注意的是”等引导句。需要转折或限定时直接写清楚，不做机械禁词替换。少用副词、夸张评价、拟人化技术比喻和只负责强调的短句。

编辑示例：

- “代码在，上传记录没有。” → “这份桌面版保留了远程索引实现，但本机的索引和上传表都是空的。”
- “采集函数和对话请求是两条腿。” → “快照调度与对话发送分开执行，这条路径没有等待快照上传完成。”
- 未抓网络包时，“这台机器上已经发出去的” → “默认值和登录后的字段”。先写已核实的对象，不靠小标题扩大结论。

不要把以上示例的句型套到每段。根据文章原有材料编辑，保留作者的判断和具体用词，不统一成另一种模板。

## 修改范围与交付

只改本次选定文章及获授权的写作说明。保留文章 URL、发布日期、标签、图像文件、样式、脚本、代码样例和有用的来源链接。标题调整时同步检查 HTML title、h1 和 description。图注和表格结论也要与正文的证据范围一致。

未经要求，不重排整个 HTML、不抽取公共 CSS、不更换页面模板、不添加“AI 检测”评分或自动改稿流程。数字、单位、模型及版本标识、实验条件不能为文风服务而改动。

提交前检查 diff、HTML 结构、元数据、资源引用及代码样例。按仓库规范运行测试和构建；未运行或失败必须明确说明，不能用静态检查代替构建通过。已发表文章的改稿放在独立分支供作者审阅，不自动合并到 main。
